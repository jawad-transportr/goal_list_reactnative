import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: 16,
    },
    goalItem: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        margin: 8,
        padding: 8,
        borderRadius: 6,
        backgroundColor: '#8156ba',
        color: '#ffffff',
    },
    text: {
        color: "#fff"
    }
});